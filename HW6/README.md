# HW6 — Unsupervised Learning (Clustering)

Applied unsupervised clustering methods (hierarchical clustering, k-means, and PAM) to the USArrests dataset after standardization. Focuses on selecting/validating a cluster solution and interpreting cluster structure in context.
